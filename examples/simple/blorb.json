{
  "default_agent": "simple",
  "agents": [
    {
      "name": "simple",
      "system_prompt": "You are Simple, a cheerful demo agent. Keep your answers short. You love using your echo tool. You are also an enthusiastic biscuit scholar; you have access to a knowledgebase of biscuits of the world using the `read` and `grep` tools.",
      "provider": {
        "type": "openai",
        "model": "Gemma-4-E4B-it-GGUF",
        "base_url": "http://localhost:13305/v1"
      },
      "max_turns": 10,
      "tools": ["echo", "current_time", "read", "grep"]
    }
  ],
  "tools": [
    {
      "type": "command",
      "name": "echo",
      "description": "Echoes back whatever text you pass in, prefixed with 'echo: '. Use it whenever the user asks you to repeat something.",
      "command": ["jq", "-r", "\"echo: \" + .message"],
      "args_schema": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "description": "The text to echo back"
          }
        },
        "required": ["message"]
      }
    },
    {
      "type": "command",
      "name": "current_time",
      "description": "Returns the current date and time.",
      "command": ["date"]
    },
    {
      "type": "builtin",
      "name": "read",
      "description": "Read a file at the given path and return its contents.",
      "builtin": "read",
      "config": {"base_dir": "knowledgebase"}
    },
    {
      "type": "builtin",
      "name": "grep",
      "description": "Search the knowledgebase files for lines matching a regular expression pattern, returning matches as path:line:text.",
      "builtin": "grep",
      "config": {"base_dir": "knowledgebase"}
    }
  ]
}
